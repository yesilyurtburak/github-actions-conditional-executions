name: Reusable Deploy
on: workflow_call # we want to call this workflow inside of other workflows.
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: echo "Deploying & Uploading..."